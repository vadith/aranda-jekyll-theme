# Vim
*~
*.sw[p_]

# Sublime Text
*.sublime-project
*.sublime-workspace

# Ruby Gem
*.gem
.bundle
Gemfile.lock
**/vendor/bundle

# Node.js and NPM
node_modules
npm-debug.log*
package-lock.json
codekit-config.json

# astro-theme/ (POC de rfc/0001-migracion-astro.md) es un proyecto Node real: su lockfile
# se versiona para builds reproducibles, a diferencia del resto del repo (que no usa npm).
!astro-theme/package-lock.json

# macOS
.DS_Store

# Jekyll generated files
.jekyll-cache
.jekyll-metadata
.sass-cache
_asset_bundler_cache
_site
